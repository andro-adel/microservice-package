<?php

use Illuminate\Support\Arr;
use Illuminate\Support\Collection;

if (! function_exists('testHelperDD')) {
    /**
     * test helper DD.
     * @return string
     */
    function testHelperDD(): string
    {
        return "hamada";
    }
}
